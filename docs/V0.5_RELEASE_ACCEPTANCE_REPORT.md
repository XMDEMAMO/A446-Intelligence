# A446 Intelligence v0.5 发布验收报告

状态：进行中（当前证据不足以放行）

验证分支：`codex/v05-parallel-integration`

报告编写基线 HEAD：`2f6a7833135a3c89d5a7b60858d90a94efe77741`

验收记录时间：2026-09-22 05:28 UTC

最终候选提交以仓库外发布证据中的完整 `releaseHead` 为准；上述 SHA 只表示本轮开始时的集成基线。

## 1 验收结论

A、B、C、D 已包含在集成基线。本轮完成 B 离线本机退出及旧 Session 恢复确认、C 可信代理配置与来源 IP 解析，并补充浏览器和 HTTP 路由回归测试。

完整 `scripts/check-all.ps1 -RequirePostgres` 已在最终工作树上通过：Hub、Server Hub、Web 构建、Mock 组合流程、浏览器 Fixture、三个生产包依赖审计和 PostgreSQL 验收全部成功。依赖审计在用户明确授权后连接 npm 官方 Registry，三个包均报告 0 个漏洞。实时浏览器门禁也已在一次性真实 Server Hub、PostgreSQL、Web 和带凭据 Worker 服务栈上通过 1/1。所有临时服务和数据均已清理。真实目标服务器和真实多设备验收仍没有可用证据，因此还不能宣布 v0.5 已通过或可发布。

## 2 本轮实现

| 范围 | 完成内容 | 关键文件 |
| --- | --- | --- |
| B：离线退出 | `offline` / `reconnecting` 时显示“本地退出”；先告知服务器 Session 尚未撤销，再清除本机身份。持久标记阻止刷新或网络恢复时自动恢复旧 Session；重新登录或确认恢复后才重新认证。 | `apps/web/src/App.tsx`、`apps/web/src/hub-api.ts`、`apps/web/src/App.css` |
| B：浏览器回归 | 覆盖 /auth/me 成功但首次业务快照失败、浏览器离线、重连、身份清除、页面刷新、旧 Session 显式恢复及离线写操作禁用。 | `tests/e2e/browser-local-logout.test.mjs`、`tests/e2e/test-support.mjs`、`tests/e2e/package.json` |
| C：来源 IP | `auth.trustedProxyIps` 默认空；实现 IPv4/IPv6 地址和 CIDR 匹配。仅 socket 对端命中配置后才解析转发链，从最近一跳向客户端跳过已信任代理；无效或过长链回退到 socket 对端。 | `apps/agent-hub/src/client-ip.mjs`、`apps/agent-hub/src/hub.mjs` |
| C：模板、文档与测试 | 配置模板新增空的 `trustedProxyIps`，补充实际代理地址、CIDR、直连和多层代理的安全说明；HTTP 登录路由测试验证配置传递及直连伪造头被忽略。 | `apps/server-hub/config/server.example.json`、`apps/server-hub/OPERATIONS.md`、`deploy/README.md`、`apps/server-hub/test/client-ip.test.mjs` |
| D：实时浏览器契约 | 真实服务栈复验发现 Workflow 创建应为 `202 Accepted`；同步修正 live 测试、Fixture 和契约断言，避免 Fixture 与生产 API 漂移。 | `tests/e2e/browser-live-release.test.mjs`、`tests/e2e/test-support.mjs`、`tests/e2e/browser-contract-api.test.mjs` |

## 3 自动检查记录

环境：Windows；Node.js 24.15.0；npm 11.12.1。检查运行于集成基线 HEAD 加本轮未提交工作树改动。

| 检查 | 结果 | 证据 |
| --- | --- | --- |
| Hub 自动测试 | 通过，35/35 | `scripts/check-all.ps1` 输出 |
| Server Hub 包检查与单元测试 | 通过，5/5；含直连、可信代理、CIDR 和无效转发链测试 | `scripts/check-all.ps1` 输出 |
| Web ESLint | 通过 | `scripts/check-all.ps1` 输出 |
| Web TypeScript 与 Vite 生产构建 | 通过 | `scripts/check-all.ps1` 输出 |
| Mock 组合端到端 Smoke | 通过；双 Worker、工作流、审批、暂停恢复、取消和事件检查均通过 | `scripts/check-all.ps1` 输出 |
| 浏览器 Fixture E2E | 通过，5/5；包含本轮两条离线退出用例 | `tests/e2e/browser-local-logout.test.mjs` 和 `scripts/check-all.ps1` 输出 |
| 实时浏览器 E2E | 通过，1/1 | 一次性真实 Server Hub + PostgreSQL + Web + 带凭据 Mock Worker；覆盖登录、身份展示、Executor 选择、创建/取消、离线禁写和服务端退出 |
| PostgreSQL 验收 | 通过，3/3 | PostgreSQL 18 一次性隔离集群；覆盖 Hub 重启恢复、身份加固、Artifact Store 与 Worker/Session 生命周期；完成后已停止并清理 |
| `git diff --check` | 通过 | 无空白错误 |
| 5173 与 8787 监听清理 | 通过 | 两个端口检查时均无监听进程 |
| 全量 `scripts/check-all.ps1 -RequirePostgres` | 通过 | 全部本地功能、浏览器、依赖审计和 PostgreSQL 门禁完成 |
| 生产依赖审计 | 通过 | 用户明确授权后访问 npm 官方 Registry；`apps/agent-hub`、`apps/server-hub`、`apps/web` 均为 0 个漏洞 |

本轮没有运行真实模型，也没有把 Mock 或 Fixture 结果记作真实模型验收。

## 4 九项发布证据状态

证据文件位于仓库外：

`C:\Users\ASUS\.codex\visualizations\2026\09\21\01a0c3d8-1ce1-72f3-9720-efe05fe7c012\a446-v05-release-evidence-2f6a783.json`

| 门槛 | 状态 | 依据 |
| --- | --- | --- |
| checkAll | 通过 | `scripts/check-all.ps1 -RequirePostgres` 完整退出 0 |
| browserE2E | 通过 | 一次性本地真实服务栈运行 live release gate，1/1 通过；临时管理员、Worker 凭据、工作流、数据库和服务均已清理 |
| postgresql | 通过 | 2026-09-22 在一次性隔离 PostgreSQL 18 集群运行 `npm.cmd run test:postgres`，3/3 通过；未使用现有数据库 |
| dependencyAudit | 通过 | 三个生产包的 `npm audit --omit=dev` 均报告 0 个漏洞 |
| backupRestore | 未执行 | 当前没有隔离目标数据库与 Artifact 目录的实际恢复演练证据 |
| versionRollback | 未执行 | 当前没有目标服务器和成对备份的版本回退演练证据 |
| workerRestart | 未执行（目标服务） | Mock Worker 故障注入通过；没有目标 systemd 服务异常退出与重连证据 |
| httpsWss | 未执行 | 当前没有目标公网测试域名及 HTTPS/WSS 现场结果 |
| realMultiDevice | 未执行 | 当前请求没有明确授权会消耗现有产品额度的真实 Codex/Antigravity 多设备调用 |

本机没有持久配置 `A446_TEST_DATABASE_URL`、`A446_E2E_BASE_URL` 或 `A446_E2E_ALLOW_MUTATION=1`。PostgreSQL 和实时浏览器验收均使用临时生成的独立服务栈，连接信息、管理员密码和 Worker Token 未写入仓库或证据文件；测试完成后已清理。真实多设备调用仍未执行。

已运行 `node tests/e2e/verify-release-evidence.mjs <仓库外证据文件>`；验证器仍以退出码 1 返回，因为备份恢复、版本回退、目标 Worker 重启、HTTPS/WSS 和真实多设备五项尚未完成。该结果与当前证据一致。

## 5 当前限制与复验

本地自动门禁和实时浏览器门禁已经通过。下一步需要目标服务器，实际完成 HTTPS/WSS、systemd、备份恢复和版本回退演练。真实多设备流程需在运行前获得明确授权。

需要人工准备的环境、逐步命令、通过标准和证据模板见 [v0.5 剩余人工操作与发布验收教程](V0.5_MANUAL_RELEASE_ACCEPTANCE_GUIDE.md)。

当前尚未完成目标环境部署。Mock Hub 继续用于本地开发；Server Hub 仍是单进程 alpha，不代表多副本高可用。
