# 本地开发默认通过 Vite 的 /api 代理访问 http://127.0.0.1:8787。
# 若前端由可信后端反向代理，可覆盖为同源 API 前缀。
VITE_HUB_API_BASE=/api

# HUB_HTTP_URL 和 HUB_TOKEN 只提供给启动 Vite 的服务端进程，
# 不要使用 VITE_ 前缀，避免凭据被打包进浏览器代码。
# HUB_HTTP_URL=http://127.0.0.1:8787
# HUB_TOKEN=server-issued-token
\n