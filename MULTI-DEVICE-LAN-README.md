# A446 多设备局域网版使用说明

## 1. 当前版本的边界

这个安装包只面向私人、可信的局域网，不使用云服务器，不使用 PostgreSQL，也不要求部署数据库。

一台 Windows 设备同时承担“协调设备”和普通执行设备两种职责：

```text
协调设备
  ├─ Local Hub：分配任务、维护群聊和流程状态
  ├─ Web 控制台：供人查看、创建任务和处理人工介入
  └─ 本机 Agent：照常参与规划、执行或审核

其他 N 台设备
  └─ 本机 Agent：主动连接协调设备
```

协调设备可通过 Windows 移动热点建立局域网，也可使用普通路由器。启动器只检查并使用现有网络，不会修改 TUN、代理、热点或网卡驱动设置。

## 2. 快速开始

所有设备解压同一个 ZIP，然后双击：

`START-A446-MULTI-DEVICE-LAN.bat`

协调设备选择：

1. `协调设备（Hub + 网页 + 本机 Agent）`
2. 首次运行确认一个全局唯一的设备名称。
3. 选择局域网 IP。Windows 移动热点通常是 `192.168.137.1`，启动器检测到时会优先使用它。
4. 选择本地执行权限。私人可信设备可选 Full；需要更严格隔离时选 Safe。
5. 启动成功后记下屏幕上的配对令牌和网页地址。本机使用 `http://127.0.0.1:5173`；其他设备使用屏幕显示的局域网地址。

其他设备选择：

1. `执行设备（连接协调设备）`
2. 输入协调设备的 IPv4 地址。
3. 输入协调设备显示的配对令牌。
4. 首次运行确认一个不与其他电脑重复的设备名称。
5. 保持启动窗口打开。

协调设备本机浏览器打开 `http://127.0.0.1:5173`；局域网内的其他设备打开协调设备显示的 LAN Console 地址。首次进入网页时输入同一个配对令牌。令牌只保存在各设备被 Git 忽略的本地目录中，不在安装包内。

首次运行会根据锁定的 `package-lock.json` 安装 Node.js 依赖，因此需要能够访问 npm。每台设备必须事先安装 Node.js 20 或更高版本，并至少安装、登录一种支持的官方客户端：Codex 或 Antigravity。

## 3. 自动识别的真实范围

启动器只使用官方客户端提供的只读接口，不读取 Cookie、浏览器资料、密码、Refresh Token、验证码或系统凭据库，也不会自动登录、退出或轮换账号。

| 项目 | Codex | Antigravity |
| --- | --- | --- |
| 当前登录态 | 自动检查 | 自动检查 |
| 账号身份 | 官方 App Server 返回；本地转换为稳定账号 ID | CLI 暂不返回可靠身份；首次由人输入非敏感账号别名 |
| 模型列表 | `model/list` 动态读取 | `agy models` 动态读取 |
| 思考强度 | 使用模型实际返回的 `supportedReasoningEfforts` | 从 CLI 模型变体/标识读取；未报告时不臆测 |
| 额度 | 官方 `account/rateLimits/read`，分别保留 5 小时和 7 天窗口 | Antigravity 1.1.12 及以上通过只读 `/usage` JSON 获取；分别保留 Gemini 与 Claude/GPT 两个模型组的 5 小时和 7 天窗口；旧版或失败时显示 Unknown |
| Token | 每轮结果上报并按任务、Agent、账号累计 | 每轮结果上报并按任务、Agent、账号累计 |

“自动识别账号”在本版本中指识别每个官方 CLI 当前激活的登录。一个客户端同时保存了多少历史账号，不等于这些账号都可被安全自动调度。自动切换账号、读取登录资料或账号轮换不在本版本范围内。

如果同一个 Codex 账号登录在多台设备上，稳定账号 ID 会自动一致。系统默认把同一账号的总并发限制为 1，避免多个逻辑 Agent 同时挤占同一个订阅账号。

Antigravity 当前不能可靠返回账号身份。若多台设备登录的是同一个 Antigravity 账号，请在首次运行时输入完全相同的账号别名；不同账号使用不同别名。否则协调端无法判断它们是否共享额度。

## 4. 设备、账号、Agent、模型的关系

系统内部严格区分四层：

```text
设备
  └─ 当前登录账号 / Provider
       └─ 可调度 Agent
            └─ 该账号当前可调用的模型与思考强度
```

本预览包为每台设备上检测到的每个 Provider 生成一个逻辑 Agent。协调设备上的 Agent 可承担规划、执行和审核；其他设备上的 Agent可承担执行和审核。Agent 是实际调度单位，设备与账号是规划 Agent 判断资源、额度和并发边界时看到的上层信息。

每个账号默认最多同时运行一个任务。即使以后为同一账号创建多个逻辑 Agent，也不会因此绕开账号并发限制。

规划 Agent 每次只收到一个紧凑的调度目录，其中包含：

- Agent ID、设备 ID和账号 ID；
- 可承担角色、在线/暂停状态和当前负载；
- 可用模型、模型实际支持的思考强度；
- 可信额度窗口及是否陈旧；
- Python、Git、GPU、Node 等本机环境摘要。

规划 Agent 可以给任务指定 Agent、模型和思考强度，也可以留空让 Hub 自动选择。Hub 会再次检查在线状态、角色、能力、模型、额度、Agent 并发和账号总并发；规划结果不能越过这些硬约束。

Antigravity 的模型会按官方额度规则分成两个池：`Gemini Models` 包含 Gemini Flash/Pro，`Claude and GPT models` 包含 Claude Opus/Sonnet 与 GPT-OSS。每个模型只继承所属池的 5 小时和 7 天额度；一个池耗尽不会误判另一个池也不可用。网页额度条显示的是“剩余比例”，所以 100% 是满格，0% 是空格。

## 5. 协作流程和上下文

一个用户任务对应一个独立群聊和一个 `rootTaskId`：

```text
规划 Agent
  → 执行 Agent：任务指令、验收标准、必要文件和最小上下文
  → 审核 Agent：执行简报、完整成果和成果附件
  → 规划 Agent：仅接收通过审核的简报、审核结论和成果引用
```

执行 Agent 发现上游结论错误时，不直接篡改计划，而是先向审核 Agent 提交问题报告。审核 Agent确认后只生成纠错简报给规划 Agent，由规划 Agent重新安排；不确认则要求执行 Agent继续原任务。

完整群聊历史不会反复发给所有模型。每个角色只读取当前步骤所需内容。每个根任务使用独立会话范围；执行返工沿用原执行会话，规划回收沿用原规划会话，不同任务不会共享模型上下文。

群聊中的 `@Agent` 主要用于人类观察和提醒，不直接改变任务状态。真正的指派、审核、返工和人工介入由结构化流程消息控制。

## 6. 本地文件存储

运行数据位于：

`apps\agent-hub\var\lan\`

主要内容：

- `hub-state.json`：任务、消息、Agent 快照、用量、介入记录和投递状态；
- `hub-state.json.bak`：最近一次可恢复备份；
- `artifacts\`：跨设备上传到协调设备的完整成果文件；
- `state\`：各逻辑 Agent 的会话、队列和处理记录；
- `provider-cache\`：短时账号、模型和额度探测缓存；
- `config\`：本机自动生成的运行配置；
- `logs\`：Hub、网页和 Worker 日志；
- `settings.json`：设备名、Hub 地址、权限模式和非敏感账号别名；
- `pairing-token.txt`：局域网配对令牌。

Agent 的工作目录位于：

`apps\agent-hub\workspaces\lan\<设备>\<Provider>\`

需要备份时，先停止 A446，再复制整个 `var\lan` 和 `workspaces\lan`。恢复时把它们放回相同版本的安装目录。不要把 `pairing-token.txt`、日志、状态或工作区提交到 GitHub 或发给他人。

## 7. Full 与 Safe

Full 模式用于你明确授权的私人设备：Codex 使用本机完整沙箱权限，A446 本地策略允许项目工作区、终端、浏览器和系统设置类任务。Safe 模式把执行限制在工作区和终端。

无论选择哪种模式，以下能力都始终拒绝：账号切换、凭据读取、Cookie/浏览器资料读取、验证码绕过、额度或平台限制绕过。

Antigravity 保持官方 `accept-edits` 自动化模式，不会加入 `--dangerously-skip-permissions`。如果某个工具仍需要授权，应在该设备上按 Antigravity 官方权限规则显式配置允许项，而不是让安装包无条件批准所有命令。

Full 模式以及局域网共享配对令牌只适用于你本人控制的可信设备。不要把端口 `5173` 或 `8787` 暴露到公网。

## 8. 启动不会消耗模型额度

启动和“仅检查”只会查询登录状态、模型列表、额度窗口和本机环境，不提交真实模型任务。只有你在网页中创建任务后，规划、执行和审核才会实际调用模型并产生 Token 消耗。

Antigravity 额度只在 CLI 版本确认不低于 1.1.12 时调用官方只读 JSON 命令；版本不明确或过旧时保持 Unknown，避免把 `/usage` 错当成普通模型提示词。

## 9. 当前已知限制

- 只有一个协调设备；它关闭后 Worker 会等待重连，任务暂时不能推进。
- 本地 JSON Store 适用于单个 Hub 进程，不能同时启动两个协调端写同一份状态。
- 共享配对令牌不是正式多用户权限系统。它适合私人局域网，不适合陌生设备或公网。
- 当前只识别每个 Provider 的当前激活账号，不自动管理同一客户端中的多个登录档案。
- Antigravity 多设备同账号依赖人输入相同别名；别名错误会导致并发统计分裂。
- 移动热点可连接设备数由 Windows、无线网卡驱动和网络环境决定，不由 A446 控制。
- 额度快照与任务 Token 是两套数据：Token 可累计，订阅额度只能显示客户端官方接口实际返回的窗口，不能用 Token 反推。
- 黑盒 CLI 正在执行的一轮模型调用无法做到 Token 级断点恢复；只能在任务阶段边界恢复。
- QQ 机器人、跨公网访问、自动故障转移和多 Hub 高可用不在本安装包内。

## 10. 常见处理

重新配对：停止 A446，删除对应设备的 `apps\agent-hub\var\lan\pairing-token.txt`，再重新启动并输入新令牌。

修改设备名、Hub IP 或权限模式：停止 A446，编辑或删除 `apps\agent-hub\var\lan\settings.json`，下次启动会重新询问。

网页显示 Agent 离线：确认对应设备启动窗口仍在、连接的是同一个热点/路由器、Hub IP 正确，并允许 Node.js 通过 Windows“专用网络”防火墙。

额度显示 Unknown：先运行菜单中的“仅检查”。Codex 需保持官方客户端登录；Antigravity 需升级到 1.1.12 以上并保持 CLI 登录。探测失败时系统会保留最后一次可信值并标记为陈旧，不会伪造百分比。

Codex 显示未登录但桌面应用已登录：同一台电脑可能同时存在旧的 `codex.cmd` 与桌面应用内置的 `codex.exe`。启动器会逐个检查可直接执行的 `codex.exe`，选择实际已登录的绝对路径，并在失败时列出每个候选入口的结果。确需手工指定时，在启动前设置用户环境变量 `A446_CODEX_EXE` 为已登录的 `codex.exe` 完整路径；不要把 Worker 改为调用 `codex.cmd`。
